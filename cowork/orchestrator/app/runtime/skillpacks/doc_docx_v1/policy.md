# DOCX Policy

1. Generate the document content.
2. Persist output as `.docx` inside the project workdir.
3. Confirm final file path and expose artifact metadata.

If conversion fails, surface the exact error and expected output contract.
